# Local Development Guide

1. Create virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Copy `.env.example` values into your local environment.
4. Run `python -m src.apis.app`.
